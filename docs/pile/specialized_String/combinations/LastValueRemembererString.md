# `LastValueRemembererString` — pure assembly; narrows `LastValueRememberer` to `String`

Source folder: `src`. Package: `pile.specialized_String.combinations`.

Pure assembly stub. Extends [`LastValueRememberer<String>`](../../aspect/LastValueRememberer.md); no new members. Re-types the last-value-rememberer contract to `String`. Not part of the `*Comparable` layering (extends the generic aspect directly, as does `LastValueRemembererBool`).

Up: [combinations index](_index.md) · [String package index](../_index.md) · [overview](../../../overview.md).
