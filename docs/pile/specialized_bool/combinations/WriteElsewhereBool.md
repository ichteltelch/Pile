# `WriteElsewhereBool` — inert/dead interface (entirely commented out)

Source folder: `src`. Package: `pile.specialized_bool.combinations`.

The entire file is commented out. It was intended to narrow the generic deferred-write combination (mirroring [`WriteElsewherePile`](../../aspect/combinations/WriteElsewherePile.md)) to `Boolean`, including a nested `WriteElsewhereBoolImpl` concrete class, but the deferred-write idea was abandoned. The file is present only to mirror the vestige in the generic `combinations` package.

No active interface, no active members.

Up: [combinations index](_index.md) · [bool package index](../_index.md) · [overview](../../../overview.md).
