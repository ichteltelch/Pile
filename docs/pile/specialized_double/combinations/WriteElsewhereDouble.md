# `WriteElsewhereDouble`

Inert: entirely commented-out, mirroring the dead generic [`WriteElsewherePile`](../../aspect/combinations/WriteElsewherePile.md) deferred-write idea.

Source folder: `src` · Package: `pile.specialized_double.combinations`

The file contains only commented-out Java: a `WriteElsewhereDouble` interface (extending `PileDouble` and `WriteElsewhere`) with a static `make(...)` factory and an inner `WriteElsewhereDoubleImpl` class. None of it compiles or is reachable.

## Tech debt
This is dead code preserved alongside the generic `WriteElsewherePile` vestige. No action required until the generic counterpart is resolved.

## See also
- [WriteElsewherePile](../../aspect/combinations/WriteElsewherePile.md) — the generic dead counterpart
- [combinations index](_index.md)
